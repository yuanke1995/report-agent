# 前端本地环境变量模板：复制为 .env.local 后按需修改
# .env.local 已被 .gitignore 排除，不会入库。

# 必须与后端 application-local.yml 的 report-agent.trusted-token 一致
VITE_AGENT_TOKEN=local-dev-token-change-me

# 模拟登录用户标识，后端按此维度做会话隔离与限流
VITE_AGENT_USER=demo
